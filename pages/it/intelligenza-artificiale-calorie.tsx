import ResourcePage from '../../components/ResourcePage'
import { SEO_LANDING_PAGES } from '../../lib/seo-landing-pages'

export default function ItalianAiCaloriesPage() {
  return <ResourcePage page={SEO_LANDING_PAGES['it-intelligenza-artificiale-calorie']} lang="it" />
}

